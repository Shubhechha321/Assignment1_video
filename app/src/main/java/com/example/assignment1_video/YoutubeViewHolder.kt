package com.example.assignment1_video

import android.R
import android.view.View
import android.widget.TextView
import androidx.recyclerview.widget.RecyclerView
import com.google.android.youtube.player.YouTubeThumbnailView

/*
class YoutubeViewHolder(itemView: View) : RecyclerView.ViewHolder(itemView) {
    var videoThumbnailImageView: YouTubeThumbnailView
    var videoTitle: TextView
    var videoDuration: TextView

    init {
        videoThumbnailImageView = itemView.findViewById(com.example.assignment1_video.R.id.video_thumbnail_image_view)
        //videoTitle = itemView.findViewById(R.id.video_title_label)
        videoTitle = itemView.findViewById(com.example.assignment1_video.R.id.video_title_label)
        videoDuration = itemView.findViewById(com.example.assignment1_video.R.id.video_duration_label)
    }
}*/